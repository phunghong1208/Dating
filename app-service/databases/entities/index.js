'use strict';

const areas = require('./areas');
const schools = require('./schools');
const jobTitles = require('./jobTitles');
const packages = require('./packages');
const reasons = require('./reasons');
const topics = require('./topics');
const customers = require('./customers');
const boosts = require('./boosts');
const activities = require('./activities');
const reports = require('./reports');
const channels = require('./channels');
const userChannels = require('./userChannels');
const messages = require('./messages');
const sockets = require('./sockets');
const devices = require('./devices');
const images = require('./images');
const imageClones = require('./imageClones');
const prompt = require('./prompt');
const promptAnswer = require('./promptAnswer');
const historyProfile = require('./historyProfile');
const reasonaccounts = require('./reasonaccounts');
const reportaccounts = require('./reportaccounts');

module.exports = {
  areas,
  schools,
  jobTitles,
  packages,
  reasons,
  topics,
  customers,
  boosts,
  activities,
  reports,
  channels,
  userChannels,
  messages,
  sockets,
  devices,
  images,
  prompt,
  promptAnswer,
  imageClones,
  historyProfile,
  reasonaccounts,
  reportaccounts,
};